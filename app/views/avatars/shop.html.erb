<div class="max-w-6xl mx-auto px-4 py-8">
  <!-- Header -->
  <div class="flex items-center justify-between mb-8">
    <div class="flex items-center gap-4">
      <%= link_to avatar_path, class: "p-2 rounded-xl bg-gray-800 hover:bg-gray-700 transition-colors" do %>
        <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
        </svg>
      <% end %>
      <div>
        <h1 class="text-3xl font-black text-white">아바타 상점</h1>
        <p class="text-gray-400">새로운 아이템을 구매하세요</p>
      </div>
    </div>
    <div class="glass-card px-4 py-2 rounded-xl">
      <p class="text-sm text-gray-400">보유 아이템</p>
      <p class="text-xl font-bold text-white"><%= @owned_item_ids.count %> / <%= AvatarItem.count %></p>
    </div>
  </div>

  <!-- Items by Category -->
  <div class="space-y-8">
    <% [['head', '머리'], ['body', '몸통'], ['background', '배경'], ['accessory', '액세서리']].each do |category, label| %>
      <div class="glass-card rounded-3xl p-6">
        <h2 class="text-xl font-bold text-white mb-6 flex items-center gap-2">
          <span class="w-8 h-8 rounded-lg bg-gradient-to-br from-purple-500 to-pink-600 flex items-center justify-center">
            <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z"/>
            </svg>
          </span>
          <%= label %>
          <span class="text-sm font-normal text-gray-400">(<%= (@items[category] || []).count %>)</span>
        </h2>

        <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-5">
          <% (@items[category] || []).sort_by { |i| ['common', 'rare', 'epic', 'legendary'].index(i.rarity) }.each do |item| %>
            <% owned = @owned_item_ids.include?(item.id) %>
            <div class="relative group">
              <div class="p-5 rounded-2xl bg-gradient-to-br <%= owned ? 'from-gray-800/80 to-gray-900/80 opacity-70' : 'from-gray-800/60 to-gray-900/60 hover:from-gray-700/60 hover:to-gray-800/60' %> border <%= item.rarity == 'legendary' ? 'border-yellow-500/30' : item.rarity == 'epic' ? 'border-purple-500/30' : 'border-gray-700/50' %> <%= owned ? '' : 'hover:border-green-500/50 hover:scale-105' %> transition-all duration-200 transform">
                <!-- Rarity glow for legendary/epic -->
                <% if item.rarity == 'legendary' && !owned %>
                  <div class="absolute inset-0 rounded-2xl bg-gradient-to-br from-yellow-500/10 to-orange-500/10 animate-pulse-slow"></div>
                <% elsif item.rarity == 'epic' && !owned %>
                  <div class="absolute inset-0 rounded-2xl bg-gradient-to-br from-purple-500/10 to-pink-500/10"></div>
                <% end %>

                <div class="relative text-center mb-4">
                  <div class="relative inline-block">
                    <span class="text-5xl drop-shadow-lg"><%= item.display_emoji %></span>
                    <% if item.rarity == 'legendary' %>
                      <div class="absolute -inset-2 animate-spin-slow opacity-30">
                        <span class="text-xl">✨</span>
                      </div>
                    <% end %>
                  </div>
                </div>

                <p class="font-bold text-white text-center text-sm truncate mb-2"><%= item.name %></p>

                <div class="flex items-center justify-center gap-2 mb-3">
                  <span class="inline-block w-2 h-2 rounded-full bg-<%= item.rarity_color %>-500 <%= item.rarity == 'legendary' ? 'animate-pulse' : '' %>"></span>
                  <span class="text-xs text-<%= item.rarity_color %>-400 font-semibold uppercase tracking-wide">
                    <%= item.rarity %>
                  </span>
                </div>

                <div class="text-center mb-3">
                  <% if item.price == 0 %>
                    <span class="inline-flex items-center gap-1 text-green-400 font-bold">
                      <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M5 5a3 3 0 015-2.236A3 3 0 0114.83 6H16a2 2 0 110 4h-5V9a1 1 0 10-2 0v1H4a2 2 0 110-4h1.17C5.06 5.687 5 5.35 5 5zm4 1V5a1 1 0 10-1 1h1zm3 0a1 1 0 10-1-1v1h1z" clip-rule="evenodd"/>
                        <path d="M9 11H3v5a2 2 0 002 2h4v-7zM11 18h4a2 2 0 002-2v-5h-6v7z"/>
                      </svg>
                      무료
                    </span>
                  <% else %>
                    <span class="inline-flex items-center gap-1 text-yellow-400 font-bold">
                      <span class="text-lg">🪙</span>
                      <%= number_with_delimiter(item.price) %>
                    </span>
                  <% end %>
                </div>

                <% if owned %>
                  <div class="text-center py-2">
                    <span class="inline-flex items-center gap-1.5 text-sm text-green-400 font-semibold">
                      <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                      </svg>
                      보유중
                    </span>
                  </div>
                <% else %>
                  <%= button_to buy_avatar_path(item_id: item.id), method: :post, class: "w-full py-2.5 px-4 bg-gradient-to-r from-green-500 to-emerald-600 text-white text-sm font-bold rounded-xl hover:shadow-lg hover:shadow-green-500/30 hover:from-green-400 hover:to-emerald-500 transition-all transform hover:-translate-y-0.5 active:translate-y-0" do %>
                    구매하기
                  <% end %>
                <% end %>
              </div>

              <!-- Tooltip with description -->
              <% if item.description.present? %>
                <div class="absolute bottom-full left-1/2 -translate-x-1/2 mb-3 px-4 py-2.5 bg-gray-900/95 backdrop-blur-sm rounded-xl text-sm text-gray-200 opacity-0 group-hover:opacity-100 transition-all duration-200 pointer-events-none whitespace-nowrap z-20 shadow-xl border border-gray-700/50">
                  <div class="flex items-center gap-2">
                    <span class="text-lg"><%= item.display_emoji %></span>
                    <span><%= item.description %></span>
                  </div>
                  <div class="absolute bottom-0 left-1/2 -translate-x-1/2 translate-y-1/2 w-3 h-3 bg-gray-900 rotate-45 border-r border-b border-gray-700/50"></div>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>

  <!-- Info Box -->
  <div class="mt-8 glass-card rounded-2xl p-6">
    <div class="flex items-start gap-4">
      <div class="w-10 h-10 rounded-full bg-blue-500/20 flex items-center justify-center flex-shrink-0">
        <svg class="w-5 h-5 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
        </svg>
      </div>
      <div>
        <h3 class="font-bold text-white mb-1">포인트 획득 방법</h3>
        <ul class="text-sm text-gray-400 space-y-1">
          <li>게시물 업로드: +100 P</li>
          <li>댓글 작성: +10 P</li>
          <li>Upvote 받기: +20 P</li>
          <li>일간 랭킹 1위: +500 P</li>
        </ul>
      </div>
    </div>
  </div>
</div>
