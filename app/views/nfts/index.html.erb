<div class="max-w-6xl mx-auto px-4 py-8">
  <!-- Header -->
  <div class="text-center mb-12">
    <div class="inline-flex items-center gap-3 mb-4">
      <div class="w-16 h-16 rounded-2xl bg-gradient-to-br from-purple-500 via-pink-500 to-orange-500 flex items-center justify-center">
        <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
        </svg>
      </div>
    </div>
    <h1 class="text-4xl font-black text-white mb-2">NFT 갤러리</h1>
    <p class="text-xl text-gray-400">모기잡이 명예의 전당</p>
    <div class="mt-4 inline-flex items-center gap-2 px-4 py-2 glass rounded-full">
      <span class="text-purple-400 font-bold"><%= @total_minted %></span>
      <span class="text-gray-400">NFTs Minted on Polygon</span>
    </div>
  </div>

  <!-- Info Banner -->
  <div class="glass-card rounded-2xl p-6 mb-8">
    <div class="flex flex-col md:flex-row items-center gap-6">
      <div class="w-20 h-20 rounded-2xl bg-gradient-to-br from-purple-600 to-pink-600 flex items-center justify-center flex-shrink-0">
        <svg class="w-10 h-10 text-white" viewBox="0 0 38 33" fill="none">
          <path d="M19 0L37.5 11V33L19 22L0.5 33V11L19 0Z" fill="currentColor" fill-opacity="0.7"/>
          <path d="M19 11L28.5 17V28L19 22L9.5 28V17L19 11Z" fill="currentColor"/>
        </svg>
      </div>
      <div>
        <h2 class="text-xl font-bold text-white mb-2">Polygon NFT</h2>
        <p class="text-gray-400">
          인기 게시물(10점 이상)은 Polygon 네트워크에서 NFT로 발행할 수 있습니다.
          발행된 NFT는 OpenSea에서 거래 가능합니다.
        </p>
      </div>
    </div>
  </div>

  <!-- NFT Grid -->
  <% if @nfts.any? %>
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @nfts.each do |nft| %>
        <%= link_to nft, class: "glass-card rounded-2xl overflow-hidden card-hover group" do %>
          <!-- Media Preview -->
          <div class="relative aspect-square bg-gray-800">
            <% if nft.post.image? %>
              <img src="<%= nft.post.media_url %>" alt="<%= nft.post.title %>" class="w-full h-full object-cover">
            <% else %>
              <video src="<%= nft.post.media_url %>" class="w-full h-full object-cover" muted></video>
            <% end %>
            
            <!-- NFT Badge -->
            <div class="absolute top-3 left-3">
              <span class="px-3 py-1 bg-gradient-to-r from-purple-500 to-pink-500 rounded-full text-xs font-bold text-white">
                NFT
              </span>
            </div>
            
            <!-- Polygon Badge -->
            <div class="absolute top-3 right-3">
              <div class="w-8 h-8 rounded-full bg-purple-600/80 flex items-center justify-center backdrop-blur-sm">
                <svg class="w-4 h-4 text-white" viewBox="0 0 38 33" fill="currentColor">
                  <path d="M19 0L37.5 11V33L19 22L0.5 33V11L19 0Z" fill-opacity="0.5"/>
                  <path d="M19 11L28.5 17V28L19 22L9.5 28V17L19 11Z"/>
                </svg>
              </div>
            </div>
          </div>
          
          <!-- Info -->
          <div class="p-4">
            <h3 class="font-bold text-white truncate mb-2"><%= nft.post.title %></h3>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-2">
                <% if nft.user.avatar_url.present? %>
                  <img src="<%= nft.user.avatar_url %>" class="w-6 h-6 rounded-full">
                <% end %>
                <span class="text-sm text-gray-400"><%= nft.user.display_name || "Anonymous" %></span>
              </div>
              <span class="text-xs text-purple-400">
                #<%= nft.token_id&.first(8) %>...
              </span>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>

    <!-- Pagination -->
    <div class="mt-8">
      <%= paginate @nfts %>
    </div>
  <% else %>
    <div class="text-center py-16">
      <div class="w-24 h-24 mx-auto mb-6 rounded-full bg-gray-800 flex items-center justify-center">
        <svg class="w-12 h-12 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
        </svg>
      </div>
      <h3 class="text-xl font-bold text-white mb-2">아직 발행된 NFT가 없습니다</h3>
      <p class="text-gray-400 mb-6">인기 게시물을 올려서 첫 번째 NFT를 발행해보세요!</p>
      <%= link_to new_post_path, class: "inline-flex items-center gap-2 px-6 py-3 bg-gradient-to-r from-purple-500 to-pink-500 text-white font-bold rounded-xl hover:shadow-lg hover:shadow-purple-500/25 transition-all" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
        </svg>
        게시물 올리기
      <% end %>
    </div>
  <% end %>
</div>
